from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
CONFIGS = ROOT / "configs"
DATASETS = ROOT / "datasets"
DATA_TEST_SETS = ROOT / "data" / "test_sets"
RESULTS = ROOT / "results"
REFERENCES = ROOT / "references"
